# Backend base URL
VITE_API_URL=http://localhost:8000

# Merchant API key — obtain by running `python backend/scripts/seed_demo.py`
# locally, or by calling POST /merchant/register directly. Required for every
# authenticated call (catalog, policy, checkout, chat, audit).
VITE_MERCHANT_API_KEY=
